import 'package:flutter/material.dart';

class FiveNumberTaskScreenText extends StatelessWidget {
  const FiveNumberTaskScreenText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Text'),
    );
  }
}
